{
  "name": "jspm",
  "description": "Frictionless browser package management",
  "homepage": "http://jspm.io/",
  "github": "https://github.com/jspm/jspm-cli",
  "npm": [
    "jspm"
  ],
  "notes": [
    "Codentity does not attribute `config.js` to jspm, as the file name is too generic.",
    "Packages that support jspm are configured using the `jspm` property in `package.json`."
  ]
}
